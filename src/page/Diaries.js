import Header from "../component/Header";

const Diaries = () => {
    return <div className="Diaries">
        <Header headText={"taste.zip"}/>
        <h2>다이어리 목록 페이지</h2>
    </div>
}

export default Diaries;